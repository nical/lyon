[package]

name = "vodk_math"
version = "0.0.1"
description = "Yet another math vector and matrix library."
license = "MIT"
authors = [ "Nicolas Silva <nical@fastmail.com>" ]
repository = "https://github.com/nical/vodk.rs"
include = [ "Cargo.toml", "src/*.rs" ]


[lib]

name = "vodkmath"
path = "src/lib.rs"
